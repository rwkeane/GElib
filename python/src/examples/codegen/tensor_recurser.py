from typing import List
import torch

from .tensor_recurser_client import TensorRecurserClient

class TensorRecurser:
  """
  This class essentially uses duck typing to mirror C++-stype SFINAE approach
  to defining torch.Tensor operations on a |child_type_|. Essentially, for
  each function, if |can_access_internals()| then the function is separately
  applied to each indidual tensor making up this instance. Else, it calls the
  function on the vec type which this class is wrapping.

  To use this class, the child must define the following functions:
  1. self._getVec() to return the SO3vecArr / SO3vec for this instance.
  2. self._createObject(results) to create a new instance of the object to be
    returned to the caller.
  3. can_access_internals() to check if the internals of this object can be
    accessed.

  NOTE: This file is autogenerated programmatically. Do not make manual changes
  as they won't persist.

  TODO: This class should allow for specific functions to not care about the
  can_access_internals() flag.
  TODO: This class should parse the real arguments, rather than realying on
  |args| and |kwargs| for each function call.
  """

  def __init__(self, child_type = TensorRecurserClient, *args, **kwargs):
    """
    |child_type| is the type of the child that extends this parent, to allow
    this class to act in a SFINAE manner.
    """
    self._child_type = child_type

  def _getParts(self) -> List[torch.Tensor]:
    return self._getVec().parts

  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def new_tensor(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().new_tensor(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.new_tensor(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def new_full(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().new_full(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.new_full(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def new_empty(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().new_empty(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.new_empty(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def new_ones(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().new_ones(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.new_ones(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def new_zeros(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().new_zeros(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.new_zeros(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def is_cuda(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().is_cuda(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.is_cuda(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def is_quantized(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().is_quantized(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.is_quantized(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def is_meta(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().is_meta(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.is_meta(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def device(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().device(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.device(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def grad(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().grad(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.grad(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def ndim(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().ndim(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.ndim(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def real(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().real(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.real(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def imag(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().imag(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.imag(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def nbytes(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().nbytes(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.nbytes(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def itemsize(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().itemsize(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.itemsize(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def abs(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().abs(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.abs(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def abs_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().abs_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.abs_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def absolute(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().absolute(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.absolute(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def absolute_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().absolute_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.absolute_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def acos(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().acos(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.acos(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def acos_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().acos_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.acos_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def arccos(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().arccos(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.arccos(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def arccos_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().arccos_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.arccos_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def add(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().add(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.add(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def add_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().add_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.add_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def addbmm(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().addbmm(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.addbmm(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def addbmm_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().addbmm_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.addbmm_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def addcdiv(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().addcdiv(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.addcdiv(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def addcdiv_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().addcdiv_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.addcdiv_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def addcmul(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().addcmul(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.addcmul(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def addcmul_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().addcmul_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.addcmul_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def addmm(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().addmm(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.addmm(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def addmm_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().addmm_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.addmm_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def sspaddmm(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().sspaddmm(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.sspaddmm(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def addmv(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().addmv(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.addmv(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def addmv_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().addmv_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.addmv_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def addr(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().addr(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.addr(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def addr_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().addr_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.addr_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def adjoint(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().adjoint(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.adjoint(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def allclose(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().allclose(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.allclose(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def amax(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().amax(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.amax(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def amin(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().amin(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.amin(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def aminmax(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().aminmax(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.aminmax(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def angle(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().angle(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.angle(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def apply_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().apply_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.apply_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def argmax(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().argmax(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.argmax(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def argmin(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().argmin(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.argmin(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def argsort(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().argsort(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.argsort(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def argwhere(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().argwhere(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.argwhere(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def asin(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().asin(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.asin(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def asin_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().asin_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.asin_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def arcsin(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().arcsin(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.arcsin(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def arcsin_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().arcsin_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.arcsin_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def as_strided(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().as_strided(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.as_strided(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def atan(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().atan(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.atan(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def atan_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().atan_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.atan_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def arctan(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().arctan(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.arctan(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def arctan_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().arctan_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.arctan_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def atan2(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().atan2(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.atan2(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def atan2_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().atan2_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.atan2_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def arctan2(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().arctan2(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.arctan2(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def arctan2_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().arctan2_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.arctan2_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def all(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().all(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.all(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def any(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().any(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.any(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def backward(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().backward(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.backward(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def baddbmm(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().baddbmm(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.baddbmm(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def baddbmm_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().baddbmm_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.baddbmm_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def bernoulli(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().bernoulli(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.bernoulli(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def bernoulli_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().bernoulli_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.bernoulli_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def bfloat16(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().bfloat16(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.bfloat16(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def bincount(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().bincount(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.bincount(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def bitwise_not(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().bitwise_not(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.bitwise_not(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def bitwise_not_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().bitwise_not_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.bitwise_not_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def bitwise_and(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().bitwise_and(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.bitwise_and(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def bitwise_and_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().bitwise_and_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.bitwise_and_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def bitwise_or(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().bitwise_or(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.bitwise_or(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def bitwise_or_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().bitwise_or_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.bitwise_or_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def bitwise_xor(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().bitwise_xor(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.bitwise_xor(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def bitwise_xor_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().bitwise_xor_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.bitwise_xor_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def bitwise_left_shift(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().bitwise_left_shift(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.bitwise_left_shift(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def bitwise_left_shift_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().bitwise_left_shift_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.bitwise_left_shift_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def bitwise_right_shift(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().bitwise_right_shift(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.bitwise_right_shift(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def bitwise_right_shift_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().bitwise_right_shift_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.bitwise_right_shift_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def bmm(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().bmm(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.bmm(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def bool(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().bool(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.bool(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def byte(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().byte(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.byte(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def broadcast_to(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().broadcast_to(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.broadcast_to(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def cauchy_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().cauchy_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.cauchy_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def ceil(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().ceil(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.ceil(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def ceil_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().ceil_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.ceil_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def char(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().char(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.char(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def cholesky(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().cholesky(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.cholesky(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def cholesky_inverse(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().cholesky_inverse(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.cholesky_inverse(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def cholesky_solve(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().cholesky_solve(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.cholesky_solve(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def chunk(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().chunk(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.chunk(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def clamp(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().clamp(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.clamp(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def clamp_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().clamp_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.clamp_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def clip(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().clip(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.clip(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def clip_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().clip_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.clip_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def clone(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().clone(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.clone(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def contiguous(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().contiguous(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.contiguous(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def copy_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().copy_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.copy_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def conj(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().conj(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.conj(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def conj_physical(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().conj_physical(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.conj_physical(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def conj_physical_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().conj_physical_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.conj_physical_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def resolve_conj(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().resolve_conj(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.resolve_conj(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def resolve_neg(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().resolve_neg(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.resolve_neg(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def copysign(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().copysign(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.copysign(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def copysign_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().copysign_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.copysign_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def cos(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().cos(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.cos(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def cos_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().cos_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.cos_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def cosh(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().cosh(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.cosh(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def cosh_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().cosh_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.cosh_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def corrcoef(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().corrcoef(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.corrcoef(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def count_nonzero(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().count_nonzero(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.count_nonzero(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def cov(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().cov(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.cov(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def acosh(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().acosh(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.acosh(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def acosh_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().acosh_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.acosh_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def arccosh(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().arccosh(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.arccosh(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def arccosh_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().arccosh_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.arccosh_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def cpu(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().cpu(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.cpu(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def cross(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().cross(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.cross(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def cuda(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().cuda(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.cuda(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def logcumsumexp(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().logcumsumexp(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.logcumsumexp(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def cummax(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().cummax(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.cummax(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def cummin(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().cummin(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.cummin(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def cumprod(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().cumprod(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.cumprod(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def cumprod_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().cumprod_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.cumprod_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def cumsum(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().cumsum(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.cumsum(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def cumsum_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().cumsum_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.cumsum_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def chalf(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().chalf(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.chalf(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def cfloat(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().cfloat(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.cfloat(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def cdouble(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().cdouble(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.cdouble(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def data_ptr(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().data_ptr(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.data_ptr(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def deg2rad(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().deg2rad(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.deg2rad(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def dequantize(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().dequantize(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.dequantize(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def det(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().det(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.det(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def dense_dim(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().dense_dim(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.dense_dim(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def detach(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().detach(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.detach(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def detach_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().detach_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.detach_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def diag(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().diag(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.diag(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def diag_embed(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().diag_embed(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.diag_embed(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def diagflat(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().diagflat(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.diagflat(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def diagonal(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().diagonal(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.diagonal(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def diagonal_scatter(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().diagonal_scatter(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.diagonal_scatter(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def fill_diagonal_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().fill_diagonal_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.fill_diagonal_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def fmax(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().fmax(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.fmax(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def fmin(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().fmin(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.fmin(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def diff(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().diff(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.diff(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def digamma(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().digamma(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.digamma(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def digamma_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().digamma_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.digamma_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def dim(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().dim(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.dim(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def dim_order(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().dim_order(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.dim_order(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def dist(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().dist(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.dist(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def div(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().div(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.div(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def div_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().div_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.div_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def divide(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().divide(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.divide(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def divide_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().divide_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.divide_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def dot(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().dot(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.dot(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def double(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().double(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.double(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def dsplit(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().dsplit(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.dsplit(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def element_size(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().element_size(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.element_size(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def eq(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().eq(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.eq(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def eq_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().eq_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.eq_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def equal(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().equal(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.equal(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def erf(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().erf(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.erf(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def erf_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().erf_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.erf_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def erfc(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().erfc(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.erfc(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def erfc_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().erfc_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.erfc_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def erfinv(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().erfinv(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.erfinv(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def erfinv_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().erfinv_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.erfinv_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def exp(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().exp(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.exp(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def exp_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().exp_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.exp_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def expm1(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().expm1(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.expm1(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def expm1_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().expm1_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.expm1_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def expand(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().expand(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.expand(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def expand_as(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().expand_as(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.expand_as(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def exponential_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().exponential_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.exponential_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def fix(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().fix(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.fix(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def fix_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().fix_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.fix_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def fill_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().fill_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.fill_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def flatten(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().flatten(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.flatten(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def flip(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().flip(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.flip(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def fliplr(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().fliplr(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.fliplr(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def flipud(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().flipud(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.flipud(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def float(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().float(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.float(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def float_power(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().float_power(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.float_power(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def float_power_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().float_power_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.float_power_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def floor(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().floor(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.floor(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def floor_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().floor_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.floor_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def floor_divide(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().floor_divide(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.floor_divide(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def floor_divide_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().floor_divide_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.floor_divide_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def fmod(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().fmod(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.fmod(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def fmod_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().fmod_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.fmod_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def frac(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().frac(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.frac(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def frac_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().frac_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.frac_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def frexp(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().frexp(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.frexp(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def gather(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().gather(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.gather(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def gcd(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().gcd(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.gcd(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def gcd_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().gcd_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.gcd_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def ge(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().ge(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.ge(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def ge_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().ge_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.ge_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def greater_equal(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().greater_equal(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.greater_equal(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def greater_equal_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().greater_equal_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.greater_equal_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def geometric_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().geometric_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.geometric_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def geqrf(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().geqrf(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.geqrf(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def ger(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().ger(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.ger(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def get_device(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().get_device(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.get_device(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def gt(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().gt(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.gt(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def gt_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().gt_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.gt_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def greater(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().greater(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.greater(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def greater_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().greater_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.greater_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def half(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().half(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.half(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def hardshrink(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().hardshrink(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.hardshrink(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def heaviside(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().heaviside(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.heaviside(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def histc(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().histc(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.histc(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def histogram(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().histogram(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.histogram(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def hsplit(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().hsplit(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.hsplit(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def hypot(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().hypot(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.hypot(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def hypot_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().hypot_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.hypot_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def i0(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().i0(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.i0(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def i0_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().i0_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.i0_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def igamma(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().igamma(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.igamma(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def igamma_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().igamma_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.igamma_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def igammac(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().igammac(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.igammac(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def igammac_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().igammac_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.igammac_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def index_add_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().index_add_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.index_add_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def index_add(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().index_add(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.index_add(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def index_copy_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().index_copy_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.index_copy_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def index_copy(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().index_copy(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.index_copy(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def index_fill_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().index_fill_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.index_fill_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def index_fill(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().index_fill(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.index_fill(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def index_put_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().index_put_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.index_put_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def index_put(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().index_put(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.index_put(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def index_reduce_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().index_reduce_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.index_reduce_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def index_reduce(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().index_reduce(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.index_reduce(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def index_select(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().index_select(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.index_select(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def indices(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().indices(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.indices(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def inner(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().inner(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.inner(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def int(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().int(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.int(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def int_repr(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().int_repr(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.int_repr(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def inverse(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().inverse(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.inverse(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def isclose(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().isclose(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.isclose(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def isfinite(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().isfinite(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.isfinite(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def isinf(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().isinf(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.isinf(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def isposinf(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().isposinf(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.isposinf(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def isneginf(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().isneginf(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.isneginf(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def isnan(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().isnan(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.isnan(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def is_contiguous(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().is_contiguous(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.is_contiguous(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def is_complex(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().is_complex(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.is_complex(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def is_conj(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().is_conj(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.is_conj(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def is_floating_point(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().is_floating_point(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.is_floating_point(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def is_inference(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().is_inference(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.is_inference(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def is_leaf(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().is_leaf(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.is_leaf(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def is_pinned(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().is_pinned(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.is_pinned(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def is_set_to(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().is_set_to(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.is_set_to(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def is_shared(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().is_shared(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.is_shared(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def is_signed(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().is_signed(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.is_signed(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def is_sparse(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().is_sparse(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.is_sparse(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def istft(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().istft(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.istft(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def isreal(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().isreal(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.isreal(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def item(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().item(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.item(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def kthvalue(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().kthvalue(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.kthvalue(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def lcm(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().lcm(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.lcm(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def lcm_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().lcm_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.lcm_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def ldexp(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().ldexp(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.ldexp(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def ldexp_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().ldexp_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.ldexp_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def le(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().le(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.le(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def le_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().le_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.le_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def less_equal(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().less_equal(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.less_equal(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def less_equal_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().less_equal_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.less_equal_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def lerp(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().lerp(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.lerp(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def lerp_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().lerp_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.lerp_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def lgamma(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().lgamma(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.lgamma(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def lgamma_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().lgamma_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.lgamma_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def log(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().log(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.log(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def log_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().log_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.log_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def logdet(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().logdet(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.logdet(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def log10(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().log10(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.log10(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def log10_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().log10_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.log10_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def log1p(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().log1p(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.log1p(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def log1p_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().log1p_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.log1p_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def log2(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().log2(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.log2(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def log2_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().log2_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.log2_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def log_normal_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().log_normal_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.log_normal_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def logaddexp(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().logaddexp(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.logaddexp(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def logaddexp2(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().logaddexp2(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.logaddexp2(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def logsumexp(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().logsumexp(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.logsumexp(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def logical_and(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().logical_and(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.logical_and(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def logical_and_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().logical_and_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.logical_and_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def logical_not(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().logical_not(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.logical_not(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def logical_not_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().logical_not_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.logical_not_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def logical_or(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().logical_or(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.logical_or(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def logical_or_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().logical_or_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.logical_or_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def logical_xor(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().logical_xor(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.logical_xor(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def logical_xor_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().logical_xor_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.logical_xor_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def logit(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().logit(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.logit(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def logit_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().logit_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.logit_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def long(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().long(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.long(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def lt(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().lt(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.lt(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def lt_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().lt_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.lt_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def less(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().less(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.less(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def less_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().less_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.less_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def lu(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().lu(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.lu(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def lu_solve(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().lu_solve(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.lu_solve(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def as_subclass(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().as_subclass(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.as_subclass(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def map_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().map_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.map_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def masked_scatter_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().masked_scatter_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.masked_scatter_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def masked_scatter(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().masked_scatter(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.masked_scatter(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def masked_fill_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().masked_fill_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.masked_fill_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def masked_fill(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().masked_fill(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.masked_fill(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def masked_select(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().masked_select(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.masked_select(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def matmul(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().matmul(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.matmul(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def matrix_power(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().matrix_power(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.matrix_power(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def matrix_exp(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().matrix_exp(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.matrix_exp(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def max(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().max(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.max(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def maximum(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().maximum(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.maximum(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def mean(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().mean(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.mean(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def module_load(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().module_load(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.module_load(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def nanmean(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().nanmean(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.nanmean(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def median(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().median(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.median(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def nanmedian(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().nanmedian(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.nanmedian(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def min(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().min(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.min(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def minimum(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().minimum(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.minimum(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def mm(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().mm(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.mm(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def smm(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().smm(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.smm(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def mode(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().mode(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.mode(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def movedim(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().movedim(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.movedim(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def moveaxis(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().moveaxis(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.moveaxis(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def msort(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().msort(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.msort(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def mul(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().mul(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.mul(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def mul_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().mul_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.mul_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def multiply(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().multiply(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.multiply(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def multiply_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().multiply_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.multiply_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def multinomial(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().multinomial(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.multinomial(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def mv(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().mv(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.mv(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def mvlgamma(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().mvlgamma(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.mvlgamma(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def mvlgamma_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().mvlgamma_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.mvlgamma_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def nansum(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().nansum(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.nansum(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def narrow(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().narrow(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.narrow(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def narrow_copy(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().narrow_copy(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.narrow_copy(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def ndimension(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().ndimension(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.ndimension(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def nan_to_num(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().nan_to_num(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.nan_to_num(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def nan_to_num_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().nan_to_num_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.nan_to_num_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def ne(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().ne(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.ne(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def ne_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().ne_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.ne_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def not_equal(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().not_equal(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.not_equal(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def not_equal_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().not_equal_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.not_equal_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def neg(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().neg(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.neg(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def neg_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().neg_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.neg_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def negative(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().negative(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.negative(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def negative_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().negative_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.negative_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def nelement(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().nelement(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.nelement(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def nextafter(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().nextafter(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.nextafter(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def nextafter_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().nextafter_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.nextafter_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def nonzero(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().nonzero(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.nonzero(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def norm(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().norm(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.norm(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def normal_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().normal_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.normal_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def numel(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().numel(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.numel(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def numpy(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().numpy(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.numpy(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def orgqr(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().orgqr(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.orgqr(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def ormqr(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().ormqr(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.ormqr(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def outer(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().outer(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.outer(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def permute(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().permute(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.permute(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def pin_memory(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().pin_memory(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.pin_memory(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def pinverse(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().pinverse(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.pinverse(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def polygamma(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().polygamma(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.polygamma(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def polygamma_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().polygamma_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.polygamma_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def positive(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().positive(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.positive(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def pow(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().pow(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.pow(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def pow_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().pow_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.pow_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def prod(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().prod(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.prod(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def put_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().put_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.put_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def qr(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().qr(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.qr(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def qscheme(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().qscheme(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.qscheme(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def quantile(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().quantile(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.quantile(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def nanquantile(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().nanquantile(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.nanquantile(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def q_scale(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().q_scale(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.q_scale(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def q_zero_point(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().q_zero_point(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.q_zero_point(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def q_per_channel_scales(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().q_per_channel_scales(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.q_per_channel_scales(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def q_per_channel_zero_points(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().q_per_channel_zero_points(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.q_per_channel_zero_points(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def q_per_channel_axis(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().q_per_channel_axis(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.q_per_channel_axis(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def rad2deg(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().rad2deg(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.rad2deg(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def random_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().random_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.random_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def ravel(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().ravel(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.ravel(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def reciprocal(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().reciprocal(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.reciprocal(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def reciprocal_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().reciprocal_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.reciprocal_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def record_stream(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().record_stream(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.record_stream(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def register_hook(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().register_hook(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.register_hook(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def register_post_accumulate_grad_hook(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().register_post_accumulate_grad_hook(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.register_post_accumulate_grad_hook(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def remainder(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().remainder(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.remainder(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def remainder_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().remainder_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.remainder_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def renorm(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().renorm(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.renorm(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def renorm_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().renorm_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.renorm_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def repeat(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().repeat(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.repeat(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def repeat_interleave(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().repeat_interleave(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.repeat_interleave(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def requires_grad(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().requires_grad(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.requires_grad(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def requires_grad_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().requires_grad_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.requires_grad_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def reshape(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().reshape(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.reshape(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def reshape_as(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().reshape_as(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.reshape_as(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def resize_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().resize_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.resize_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def resize_as_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().resize_as_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.resize_as_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def retain_grad(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().retain_grad(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.retain_grad(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def retains_grad(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().retains_grad(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.retains_grad(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def roll(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().roll(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.roll(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def rot90(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().rot90(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.rot90(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def round(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().round(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.round(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def round_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().round_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.round_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def rsqrt(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().rsqrt(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.rsqrt(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def rsqrt_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().rsqrt_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.rsqrt_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def scatter(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().scatter(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.scatter(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def scatter_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().scatter_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.scatter_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def scatter_add_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().scatter_add_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.scatter_add_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def scatter_add(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().scatter_add(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.scatter_add(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def scatter_reduce_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().scatter_reduce_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.scatter_reduce_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def scatter_reduce(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().scatter_reduce(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.scatter_reduce(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def select(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().select(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.select(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def select_scatter(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().select_scatter(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.select_scatter(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def set_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().set_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.set_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def share_memory_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().share_memory_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.share_memory_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def short(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().short(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.short(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def sigmoid(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().sigmoid(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.sigmoid(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def sigmoid_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().sigmoid_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.sigmoid_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def sign(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().sign(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.sign(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def sign_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().sign_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.sign_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def signbit(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().signbit(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.signbit(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def sgn(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().sgn(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.sgn(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def sgn_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().sgn_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.sgn_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def sin(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().sin(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.sin(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def sin_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().sin_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.sin_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def sinc(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().sinc(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.sinc(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def sinc_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().sinc_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.sinc_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def sinh(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().sinh(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.sinh(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def sinh_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().sinh_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.sinh_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def asinh(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().asinh(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.asinh(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def asinh_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().asinh_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.asinh_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def arcsinh(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().arcsinh(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.arcsinh(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def arcsinh_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().arcsinh_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.arcsinh_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def shape(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().shape(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.shape(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def size(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().size(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.size(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def slogdet(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().slogdet(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.slogdet(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def slice_scatter(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().slice_scatter(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.slice_scatter(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def softmax(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().softmax(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.softmax(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def sort(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().sort(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.sort(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def split(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().split(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.split(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def sparse_mask(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().sparse_mask(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.sparse_mask(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def sparse_dim(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().sparse_dim(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.sparse_dim(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def sqrt(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().sqrt(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.sqrt(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def sqrt_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().sqrt_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.sqrt_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def square(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().square(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.square(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def square_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().square_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.square_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def squeeze(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().squeeze(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.squeeze(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def squeeze_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().squeeze_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.squeeze_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def std(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().std(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.std(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def stft(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().stft(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.stft(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def storage(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().storage(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.storage(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def untyped_storage(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().untyped_storage(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.untyped_storage(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def storage_offset(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().storage_offset(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.storage_offset(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def storage_type(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().storage_type(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.storage_type(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def stride(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().stride(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.stride(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def sub(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().sub(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.sub(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def sub_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().sub_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.sub_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def subtract(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().subtract(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.subtract(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def subtract_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().subtract_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.subtract_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def sum(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().sum(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.sum(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def sum_to_size(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().sum_to_size(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.sum_to_size(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def svd(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().svd(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.svd(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def swapaxes(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().swapaxes(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.swapaxes(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def swapdims(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().swapdims(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.swapdims(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def t(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().t(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.t(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def t_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().t_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.t_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def tensor_split(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().tensor_split(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.tensor_split(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def tile(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().tile(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.tile(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def to(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().to(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.to(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def to_mkldnn(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().to_mkldnn(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.to_mkldnn(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def take(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().take(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.take(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def take_along_dim(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().take_along_dim(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.take_along_dim(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def tan(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().tan(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.tan(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def tan_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().tan_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.tan_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def tanh(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().tanh(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.tanh(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def tanh_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().tanh_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.tanh_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def atanh(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().atanh(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.atanh(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def atanh_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().atanh_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.atanh_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def arctanh(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().arctanh(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.arctanh(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def arctanh_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().arctanh_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.arctanh_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def tolist(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().tolist(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.tolist(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def topk(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().topk(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.topk(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def to_dense(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().to_dense(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.to_dense(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def to_sparse(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().to_sparse(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.to_sparse(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def to_sparse_csr(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().to_sparse_csr(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.to_sparse_csr(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def to_sparse_csc(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().to_sparse_csc(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.to_sparse_csc(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def to_sparse_bsr(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().to_sparse_bsr(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.to_sparse_bsr(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def to_sparse_bsc(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().to_sparse_bsc(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.to_sparse_bsc(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def trace(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().trace(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.trace(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def transpose(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().transpose(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.transpose(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def transpose_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().transpose_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.transpose_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def triangular_solve(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().triangular_solve(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.triangular_solve(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def tril(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().tril(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.tril(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def tril_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().tril_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.tril_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def triu(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().triu(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.triu(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def triu_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().triu_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.triu_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def true_divide(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().true_divide(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.true_divide(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def true_divide_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().true_divide_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.true_divide_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def trunc(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().trunc(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.trunc(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def trunc_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().trunc_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.trunc_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def type(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().type(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.type(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def type_as(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().type_as(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.type_as(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def unbind(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().unbind(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.unbind(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def unflatten(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().unflatten(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.unflatten(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def unfold(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().unfold(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.unfold(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def uniform_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().uniform_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.uniform_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def unique(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().unique(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.unique(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def unique_consecutive(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().unique_consecutive(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.unique_consecutive(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def unsqueeze(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().unsqueeze(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.unsqueeze(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def unsqueeze_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().unsqueeze_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.unsqueeze_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def values(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().values(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.values(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def var(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().var(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.var(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def vdot(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().vdot(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.vdot(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def view(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().view(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.view(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def view_as(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().view_as(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.view_as(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def vsplit(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().vsplit(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.vsplit(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def where(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().where(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.where(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def xlogy(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().xlogy(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.xlogy(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def xlogy_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().xlogy_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.xlogy_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def zero_(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().zero_(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.zero_(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def __str__(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().__str__(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.__str__(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def __len__(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().__len__(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.__len__(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def __add__(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().__add__(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.__add__(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def __call__(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().__call__(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.__call__(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def __add__(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().__add__(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.__add__(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def __sub__(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().__sub__(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.__sub__(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def __mul__(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().__mul__(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.__mul__(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def __pow__(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().__pow__(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.__pow__(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def __truediv__(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().__truediv__(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.__truediv__(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def __floordiv__(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().__floordiv__(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.__floordiv__(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def __mod__(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().__mod__(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.__mod__(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def __lshift__(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().__lshift__(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.__lshift__(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def __rshift__(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().__rshift__(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.__rshift__(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def __and__(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().__and__(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.__and__(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def __or__(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().__or__(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.__or__(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def __xor__(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().__xor__(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.__xor__(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def __invert__(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().__invert__(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.__invert__(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def __lt__(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().__lt__(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.__lt__(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def __le__(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().__le__(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.__le__(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def __eq__(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().__eq__(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.__eq__(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def __ne__(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().__ne__(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.__ne__(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def __gt__(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().__gt__(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.__gt__(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  
  # TODO: Uncomment the self.can_access_internals() check once this class can
  # handle differentiating equivariant and non-equivariant functions.
  def __ge__(self, *args, **kwargs):
    # If the internals are inaccessible, treat this as a call on SO3vecArr.
    # if not self.can_access_internals():
    #   # Modify all |args| that are PointCloud instances to just be the
    #   # specific part we care about.
    #   args = list(args)
    #   for i, arg in enumerate(args):
    #     if isinstance(arg, self._child_type):
    #       args[i] = arg._getVec()

    #   # Do the same for |kwargs|.
    #   for key, value in kwargs.items():
    #     if isinstance(value, self._child_type):
    #       kwargs[key] = value._getVec()

    #   return self._getVec().__ge__(*args, **kwargs)

    # # Else, try to propegate to the underlying parts as tensors.
    # else:
      results = []
      gelib_vec_parts = self._getParts()

      # Calculate the result for each individual part.
      for i in range(len(gelib_vec_parts)):
        gelib_vec_part = gelib_vec_parts[i]

        # Modify all |args| that are PointCloud instances to just be the
        # specific part |gelib_vec_part| we care about.
        part_args = list(args)
        for i, arg in enumerate(args):
          if isinstance(arg, self._child_type):
            part_args[i] = arg._getParts()[i]
          elif isinstance(arg, torch.Tensor):
            assert arg.size()[-2] == 1
            new_size = list(arg.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_args[i] = arg.expand(tuple(new_size))

        # Do the same for |kwargs|.
        part_kwargs = kwargs.copy()
        for key, value in kwargs.items():
          if isinstance(value, self._child_type):
            part_kwargs[key] = value._getParts()[i]
          elif isinstance(value, torch.Tensor):
            assert value.size()[-2] == 1
            new_size = list(value.size())
            new_size[-2] = gelib_vec_part.size()[-2]
            part_kwargs[key] = arg.expand(tuple(new_size))

        # Call the underlying function with modifies parameters.
        results.append(gelib_vec_part.__ge__(*part_args, **part_kwargs))

      # Turn it into an object and return it
      if results[0] == None:
        all_nones = True
        for i in range(1, len(results)):
          all_nones &= (results[i] == None)
        if all_nones:
          return None
      elif isinstance(results[0], torch.Tensor):
        return self._createObject(results)
      
      return results
  